# Dependencias y builds (se instalan/generan dentro de la imagen)
node_modules
.next
out
build
coverage

# Git y IDE
.git
.gitignore
.vscode
.idea

# Entorno local
.env
.env.*
!.env.example

# Documentación y evidencias (no necesarias en la imagen)
*.md
INFORME.md
evidencias
.pdf-check
.pdf-pages

# Docker / compose (evita copiar archivos meta al contexto)
docker-compose.yml
docker-compose.*.yml
Dockerfile
.dockerignore

# Logs y misc
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.DS_Store
pnpm-lock.yaml

# Jupyter (servicio separado; no va en stress_monitor)
jupyter
